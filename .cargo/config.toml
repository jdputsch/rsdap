# Platform-specific linker overrides for faster incremental builds.
# macOS uses its default ld (ld_prime / ld64) — no override needed.
# Uncomment on Linux if lld is installed:
# [target.x86_64-unknown-linux-gnu]
# linker = "clang"
# rustflags = ["-C", "link-arg=-fuse-ld=lld"]
